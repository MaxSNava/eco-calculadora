/** Cálculos */
import type { AppData } from "../app/schemas";

/* TODO 
  calcular todo por 30 dolares por tonelada
*/
export function calcularResultados(data: AppData) {
  console.log(data);
  return { numeriot: 0 };
}
